<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Individ_project">

        <!-- Мини-игра (ДОБАВЛЕНО) -->
        <activity
            android:name=".StackGameActivity"
            android:exported="false"
            android:screenOrientation="portrait"/> <!-- Лучше зафиксировать портретную ориентацию -->

        <!-- Активити результатов -->
        <activity
            android:name="com.example.individ_project.ResultActivity"
            android:exported="false" />

        <!-- Активити квиза -->
        <activity
            android:name=".QuizActivity"
            android:exported="false" />

        <!-- Главная -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
